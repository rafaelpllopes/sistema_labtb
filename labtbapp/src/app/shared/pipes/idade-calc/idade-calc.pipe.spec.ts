import { IdadeCalcPipe } from './idade-calc.pipe';

describe('IdadeCalcPipe', () => {
  it('create an instance', () => {
    const pipe = new IdadeCalcPipe();
    expect(pipe).toBeTruthy();
  });
});
